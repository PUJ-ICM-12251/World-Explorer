package com.example.proyectocm;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;

public class menu extends AppCompatActivity {
/*

    Button btnMapa, btnCrearLugar, btnqr, btnUsuario;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_menu);

        btnMapa = findViewById(R.id.btnMapa);

        btnCrearLugar = findViewById(R.id.btnCrearLugar);

        btnMapa.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent map = new Intent(menu.this, MapActivity.class);
                startActivity(map);
            }
        });

        btnCrearLugar.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent CrearLugar = new Intent(menu.this, CrearLugarActivity.class);
                startActivity(CrearLugar);
            }
        });

        btnqr=(Button) findViewById(R.id.btn_qr);

        btnqr.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick(View view) {
                Intent i = new Intent( MainActivity.this, qr.class);
                startActivity(i);
            }
        });

        btnUsuario = findViewById(R.id.btn_usuario);

        btnUsuario.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick(View view) {
                Intent i = new Intent( MainActivity.this, qr.class);
                startActivity(i);
            }
        });


    }*/
}